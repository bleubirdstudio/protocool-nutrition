<div id="page" class="container-fluid no-pad">
  
  <div class="span12 details">
    <div class="ban pone"><h1 class="p25">{{ page.title }}</h1></div>
    <div class="container">{{ page.content }}</div>
  </div>
  
</div>